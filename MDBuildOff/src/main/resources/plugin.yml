name: BuildOff
version: '${project.version}'
main: buildoff.buildoff.BuildOff
api-version: 1.20
depend: [MDTeams]
softdepend: [MDTeams]
commands:
  buildoff:
    description: buildoff master command
    usage: /<command>
    permission: buildoff